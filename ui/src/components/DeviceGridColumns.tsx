import { GridColDef } from '@mui/x-data-grid';
import { Box, Typography, CircularProgress, Chip } from '@mui/material';

export const getDeviceColumns = (): GridColDef[] => [
  // ... existing column definitions but with better typing
]; 